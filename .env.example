# Shared secret between grafana and grafana-image-renderer.
# Required since grafana-image-renderer >= 4.0 — Grafana refuses to boot
# if this is unset or left at the upstream default.
# Generate with: openssl rand -hex 32
GRAFANA_RENDERER_TOKEN=
